let Arr = ['a', 2, 4, 5, 'h'];
Arr.forEach((item, index) => {
        // item.splice(1, index);
        // item.filter(item)
        console.log(item)
    })
    // Arr1.push(item)

let Arr1 = ['aa', 2, 5, 6, 4];
Arr1.forEach((item, index) => {
    item[index].filter(0, index)
    console.log(item)
})